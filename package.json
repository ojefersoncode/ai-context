{
  "name": "@ojefersoncode/ai-context-cli",
  "private": true,
  "version": "0.0.0",
  "description": "AI Context — CLI que gera contexto de projeto para modelos de IA",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=ai-context-cli",
    "dev": "npm run dev --workspace=ai-context-cli",
    "typecheck": "npm run typecheck --workspace=ai-context-cli"
  }
}
